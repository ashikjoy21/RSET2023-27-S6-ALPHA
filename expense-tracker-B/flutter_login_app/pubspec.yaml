name: expense_tracker
description: A Flutter expense tracking application with admin module

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  cupertino_icons: ^1.0.2

  permission_handler: ^11.3.1
  intl: ^0.20.2
  fl_chart: ^1.1.1
  table_calendar: ^3.2.0
  flutter_local_notifications: ^21.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
